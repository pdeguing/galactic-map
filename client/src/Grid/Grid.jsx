import React from "react"

import Cell from "../Cell"

import styles from "./Grid.module.css"

export default ({ gameState }) => {
	return <div className={styles.Grid} style={{"width": `$galaxyMap.length * 20}px`}}>

}
